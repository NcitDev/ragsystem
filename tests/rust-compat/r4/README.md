# Phase R4 Rust Compatibility Fixtures

This directory captures Python-oracle output for deterministic retrieval and
repo-agent evidence-bundle helpers. The Rust tests read these fixtures as
goldens and compare normalized fields only.
